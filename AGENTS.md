# Expo HAS CHANGED

This checkout uses Expo SDK 54 (`expo ~54.0.37`, React Native 0.81.5). Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code, and verify `package.json` plus `npx expo config` if dependencies change.
